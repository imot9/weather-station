MACHINE = "raspberrypi5"
DISTRO = "nodistro"
INHERIT += "rm_work"
IMAGE_INSTALL:append = "wpa-supplicant iw openssh pciutils kernel-modules"
EXTRA_IMAGE_FEATURES ?= "allow-empty-password empty-root-password allow-root-login"
